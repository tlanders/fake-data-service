server:
  port: 8888

spring:
  application:
    name: FakeDataService
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9001

logging:
  level:
    root: ERROR
    org:
      springframework:
        web: ERROR
        security: INFO
    com:
      auth0: TRACE
    biz:
      lci: TRACE